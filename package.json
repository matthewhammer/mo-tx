{
  "name": "mo-tx",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "format": "prettier --write \"src/**/*.{json,mo}\"",
    "sources": "mops sources",
    "prepare": "husky install",
    "precommit": "lint-staged",
    "postinstall": "mops install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^2.8.8",
    "prettier-plugin-motoko": "^0.6.1"
  },
  "lint-staged": {
    "src/**/*.{js,ts,jsx,tsx}": [
      "eslint --max-warnings=0 --fix",
      "prettier --write"
    ]
  }
}
